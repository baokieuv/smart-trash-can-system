spring:
  application:
    name: smart-bin-server

  datasource:
    url: jdbc:mariadb://localhost:3333/smart-bin
    username: admin
    password: admin
    driver-class-name: org.mariadb.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      connection-timeout: 20000
      maximum-pool-size: 10
      minimum-idle: 5
      max-lifetime: 1200000
      idle-timeout: 300000
      leak-detection-threshold: 60000
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8080/realms/smart-bin
          jwk-set-uri: http://localhost:8080/realms/smart-bin/protocol/openid-connect/certs
  mail:
    host: smtp.gmail.com
    port: 465
    username: kieubao2k4@gmail.com
    password: srmg hvpl rebq hrpn
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          trust: smtp.gmail.com
server:
  port: 8888


app:
  ai-server:
    url: http://localhost:8000
  email:
    from: kieubao2k4@gmail.com
    verification:
      url: http://www.kvbhust.site/api/v1/auth/verify-email

keycloak:
  server-url: http://localhost:8080
  realm: smart-bin
  client-id: smart-bin-client
  client-secret: wYhiGkc0Pgw1vrS6HRcuZ6ChzH9GmT3Y
  admin-username: admin
  admin-password: admin
